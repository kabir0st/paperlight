# Dependencies, reinstalled with `npm install` before `npm run build`.
# The build *output* (offscreen.js, tts-worker.js, vendor/) is committed on
# purpose so load-unpacked works without Node, do not add it here.
node_modules/

# npm / logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm/

# Editor & IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS metadata
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
desktop.ini

# Local assistant state
.claude/
